EMPLOYEE WSL PASSWORD VERIFICATION FLOW
==========================================

1. EMPLOYEE ENTERS PASSWORD
   └── WslPasswordPrompt.jsx (line 51)
       └── testWslRootCredentials(password)

2. PASSWORD MANAGER CALLS MAIN PROCESS
   └── wslPasswordManager.js (line 140)
       └── window.cyberGuard.testWslRootCredentials(password)

3. MAIN PROCESS EXECUTES WSL COMMAND
   └── main.js (line 712)
       └── echo "${password}" | wsl -d kali-linux su - root -c "whoami"

4. WSL SYSTEM VERIFIES PASSWORD
   └── Linux su command checks password against system
       ├── SUCCESS: Returns "root"
       └── FAILURE: Returns empty + "Authentication failure"

5. RESULT RETURNED TO UI
   ├── SUCCESS: Password stored, success message shown
   └── FAILURE: Error message shown, password not stored

DEBUG LOGS SHOW:
================

🔐 [WSL-ROOT-AUTH] Starting root credential verification...
🔐 [WSL-ROOT-AUTH] Password length: X
🔐 [WSL-ROOT-AUTH] Full command (DEBUG): echo "ACTUAL_PASSWORD" | wsl -d kali-linux su - root -c "whoami"
🔐 [WSL-ROOT-AUTH] Command execution completed in X ms
🔐 [WSL-ROOT-AUTH] STDOUT: "root" (SUCCESS) or "" (FAILURE)
🔐 [WSL-ROOT-AUTH] STDERR: "" (SUCCESS) or "su: Authentication failure" (FAILURE)
✅ [WSL-ROOT-AUTH] WSL root credentials VALID (SUCCESS)
❌ [WSL-ROOT-AUTH] WSL root credentials INVALID (FAILURE)

MANUAL TEST COMMAND:
===================

echo "employee_password" | wsl -d kali-linux su - root -c "whoami"

Expected Results:
- SUCCESS: Output = "root"
- FAILURE: No output + error message
